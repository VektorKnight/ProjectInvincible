using UnityEngine;

namespace InvincibleEngine.Components.Generic {
	/// <summary>
	/// Used for the kill feed portion of the player hud.
	/// Handles fading in and out of the element along with the desired text.
	/// </summary>
	public class KillIndicator : MonoBehaviour {
		
		// Unity Inspector (Settings)
		private float _fadeInTime = 0.5f;
		private float _fadeOutTime = 0.25f;

		// Use this for initialization
		void Start () {
		
		}
	
		// Update is called once per frame
		void Update () {
		
		}
	}
}
